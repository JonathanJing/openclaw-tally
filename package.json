{
  "name": "openclaw-task-metrics",
  "version": "0.1.0",
  "type": "module",
  "description": "Task-level efficiency analytics for OpenClaw",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "vitest run",
    "migrate": "node -e \"import('./src/ledger.js').then(m => new m.TaskLedger().init())\""
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
